export type BannerItemData = {
  imageUrl: string;
};

export type MapItemData = {
  name: string;
  x: number;
  y: number;
  description: string;
};

export type ExhibitionItemData = {
  title: string;
  description: string;
  date?: string;
  imageUrl: string;
};